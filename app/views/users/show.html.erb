<p id="notice"><%= notice %></p>

<ul class="nav nav-tabs">
	<li class="active"><a href="#home" data-toggle="tab">我的HH</a></li>
	<li><a href="#profile" data-toggle="tab">账号</a></li>
	<li><a href="#messages" data-toggle="tab">收件箱</a></li>
</ul>
<div class="tab-content">
	<div class="tab-pane active" id="home">
		<p>
			<b>Email:</b>
			<%= @user.email %>
		</p>
		<% unless @user.route_records.empty? %>
		<table class="route_records" summary="User route_record">
			<%= render @route_records %>
		</table>
		<%= will_paginate @route_records %>
		<% end %>
	</div>
	<div class="tab-pane" id="profile">
		<p>profile page</p>
	</div>
	<div class="tab-pane" id="messages">
		<p>messages content</p>
	</div>
</div>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
