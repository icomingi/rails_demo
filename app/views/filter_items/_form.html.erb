<%= form_for(@filter_item) do |f| %>
  <% if @filter_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@filter_item.errors.count, "error") %> prohibited this filter_item from being saved:</h2>

      <ul>
      <% @filter_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :driver_id %><br />
    <%= f.number_field :driver_id %>
  </div>
  <div class="field">
    <%= f.label :result_id %><br />
    <%= f.number_field :result_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
